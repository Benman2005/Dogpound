import React, { PureComponent } from 'react';

export default class StartPage extends PureComponent {
    render(){

        return (
        <div>
            HOMEPAGE
        </div>
        )
    }
}